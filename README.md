math and logic are available at math_experiment_result.json
image recognition are available at image_experiment_result.json

in the .ipynb code, I used two different API keys because Google AI Studio limits one project to 20 requests per day.
With 30 total prompts, its easier to just use two API keys
